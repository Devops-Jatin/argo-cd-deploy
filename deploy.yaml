apiVersion: apps/v1
kind: Deployment
metadata: 
  name: mydeploy
spec: 
  replicas: 2
  selector: 
    matchLabels:
      app: mydeploy
  template: 
    metadata: 
      labels: 
        app: mydeploy
    spec: 
      containers:
        - name: mydeploy
          image: nginx
          ports: 
            - containerPort: 80
            